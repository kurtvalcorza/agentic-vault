Idea: weekly review could auto-generate from session logs — the logs already record what changed per day, so a "compile my week" skill just needs to aggregate them and group by project.

Maybe relates to the Kanban board's Complete column too?

#status/inbox
