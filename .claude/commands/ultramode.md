---
description: Activate Ultramode — the vault's agent operating standard (verify-first, re-verify sub-agents, decision-gate, terse-and-numeric) for the rest of this session.
argument-hint: [optional task to do under Ultramode]
---

# Ultramode

**Read `.agent/steering/ultramode.md` now and adopt it as your operating method
for the rest of this session.** It layers on `AGENTS.md` (protocol wins) and
[[voice]] + [[anti-style]] (voice wins), and buys auditability plus an edge-case
margin — not baseline correctness.

Then, in **one line**, confirm the disciplines you're now holding to — above all
**§0**: treat every sub-agent result, "COMPLETE", summary, README, or second-hand
claim as a *hypothesis* and re-verify it against the source before acting.

If `$ARGUMENTS` is non-empty, proceed with that task under this standard;
otherwise wait for my next request.
