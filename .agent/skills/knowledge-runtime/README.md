# knowledge-runtime skill

Agent-agnostic guidance for using the optional `.agent/knowledge/` subsystem.

Use this skill for typed knowledge lookup, relation/path traversal, provenance and timeline inspection, reconciliation, health checks, and proposal-first semantic mutations. It intentionally does not depend on a particular agent vendor or MCP client.

The runtime is optional. If unavailable, agents should fall back to ordinary vault search/read workflows.
