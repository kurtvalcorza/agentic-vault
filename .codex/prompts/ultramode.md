Read `.agent/steering/ultramode.md` (relative to the current working directory — run this from the vault root) now and adopt it as your operating method for the rest of this session. It layers on `AGENTS.md` (protocol wins) and the `voice` + `anti-style` steering (voice wins), and buys auditability plus an edge-case margin — not baseline correctness.

Then, in one line, confirm the disciplines you're now holding to — above all §0: treat every sub-agent result, "COMPLETE", summary, README, or second-hand claim as a hypothesis and re-verify it against the source before acting.

If $ARGUMENTS is non-empty, proceed with that task under this standard; otherwise wait for my next request.
